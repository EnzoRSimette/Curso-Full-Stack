/*

var resultado = (condição) ? valor1 : valor2 
? Pergunta se é verdadeiro
Se for, valor 1
Se não for, valor 2

*/

let enviar_email = false
let frase = enviar_email ? "enviar" : "não enviar"
console.log("frase")
